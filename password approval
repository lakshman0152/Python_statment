def user_password(password):
    if len(password)<8:
        return("weak password")
    elif len(password)>15:
        return ("too big paasword")
    else:
    return("strong password")    

password = input("enter ur password : ")
print(user_password(password))
